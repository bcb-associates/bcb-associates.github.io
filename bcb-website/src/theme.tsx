import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#6039b4",
    },
    secondary: {
      main: "#ffffff",
    },
  },
});
export default theme;