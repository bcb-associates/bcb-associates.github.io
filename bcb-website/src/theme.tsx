import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "rgba(33,39,191,.5)",
    },
    secondary: {
      main: "rgba(212,175,55,1)",
    },
  },
});
export default theme;
